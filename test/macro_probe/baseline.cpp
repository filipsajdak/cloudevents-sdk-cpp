// Preprocessed with -dM -E under the same flags as headers.cpp, and including
// nothing: its CE_ macros are the ones the command line defines (a preset's
// -DCE_EXPECT_REFLECTION, say), which the headers are not answerable for.
